// Pointer to the currently-selected mode. Rewritten by pick_mode.bat.
// server.cfg should `exec current_mode` so this re-applies on every map load,
// making the picked mode persist across Versus chapters.
// Defaults to a vanilla reset until a mode is picked.
exec reset
